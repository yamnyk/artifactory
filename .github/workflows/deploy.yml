name: Deploy Artifactory

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Enable corepack and install pnpm
        run: |
          corepack enable
          corepack prepare pnpm@8 --activate

      - name: Install dependencies
        run: pnpm install

      - name: Build for GitHub Pages
        run: |
          pnpm run build:gh
          cp dist/index.html dist/404.html
          mv dist dist-gh

      - name: Build for Ukraine.com.ua
        run: |
          pnpm run build
          mv dist dist-ua

      - name: Upload GH Pages build
        uses: actions/upload-artifact@v4
        with:
          name: gh-build
          path: dist-gh

      - name: Upload Ukraine build
        uses: actions/upload-artifact@v4
        with:
          name: ua-build
          path: dist-ua

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: production

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Download GH Pages build
        uses: actions/download-artifact@v4
        with:
          name: gh-build
          path: dist-gh

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: gh-build

      - name: Download Ukraine build
        uses: actions/download-artifact@v4
        with:
          name: ua-build
          path: dist-ua

      - name: Deploy to Ukraine.com.ua
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          source: 'dist-ua/**'
          target: '~/yamnyk.site/artifactory/'
          strip_components: 1
